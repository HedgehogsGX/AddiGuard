I will assist you in initializing the Git repository and preparing the code for upload. Since the `gh` (GitHub CLI) tool is not installed in the current environment, I cannot automatically create the repository on GitHub for you. We will proceed in two stages:

### Phase 1: Local Setup (I will perform these steps)
1.  **Initialize Git Repository**: Run `git init` in the project root.
2.  **Create .gitignore**: Create a root `.gitignore` file to exclude unnecessary files (like `__pycache__`, `instance/` database files, `.DS_Store`, etc.) to ensure a clean upload.
3.  **Commit Code**: Add all project files and create the initial commit.

### Phase 2: Upload to GitHub (Requires your participation)
After I complete Phase 1, you will need to:
1.  Log in to [GitHub](https://github.com/new) and create a new repository named **AddiGuard**.
2.  Do **not** initialize it with a README, .gitignore, or license (since we already have the code locally).
3.  Copy the repository URL (e.g., `https://github.com/yourusername/AddiGuard.git`).
4.  Paste the URL back to me, and I will configure the remote and push the code for you.
